targetEnv=#{targetEnv}
zookeeper.url=#{zookeeper.url}
dubbo.port=#{dubbo.port}

task.fixed.delay=300000

log4jPrefix=#{log4jPrefix}

log4j.rootPath=#{log4jPrefix}


jdbc.driverClassName=#{jdbc.driverClassName}
jdbc.url=#{jdbc.url}
jdbc.username=#{jdbc.username}
jdbc.password=#{jdbc.password}
jdbc.initialSize=10
jdbc.maxActive=100
jdbc.numTestsPerEvictionRun=100
jdbc.maxIdle=10